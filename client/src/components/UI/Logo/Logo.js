// import image from "../assets/images/logo.png";

const Logo = ({ onClick }) => {
  return (
    <div onClick={onClick}>
      Logo
      {/* <img src={image} alt="Logo" /> */}
    </div>
  );
};

export default Logo;
